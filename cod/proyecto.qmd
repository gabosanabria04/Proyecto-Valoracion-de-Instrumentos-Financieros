---
title: "Evaluación Actuarial - Smart Investments S.A."
author:
- Paola Espinoza Hernández
- Gabriel Sanabria Alvarado
- Joseph Romero Chinchilla
output:
  pdf_document:
    latex_engine: xelatex
    toc: yes
    number_sections: yes
  html_document:
    toc: yes
    df_print: paged
---

```{r cargar curva cero cupón}
curva <- read.csv("../data/Curva_0C.csv", sep = ";", col.names = c("vencimiento", "colones", "dolares"), dec = ",")
curva$vencimiento <- as.numeric(curva$vencimiento)
curva$colones     <- as.numeric(curva$colones)
curva$dolares     <- as.numeric(curva$dolares)
```

# Funciones Utilizadas
## Precios Forward
```{r, echo = FALSE}
set.seed(123) 
```

```{r, calcular precio forward}
forward <- function(t, S, T, moneda){
  if(t == 0){ #versión normal
    idS <- which.min(abs(curva$vencimiento - S))
    idT <- which.min(abs(curva$vencimiento - T))
    rhoS <- curva[[moneda]][idS]/100
    rhoT <- curva[[moneda]][idT]/100
    PS <- ((1+rhoS)^-S)
    PT <- ((1+rhoT)^-T)
    return(PT/PS)
  }
}
```

## Tasas cortas
```{r}
# Función para generar árbol de tasas de interés
# Recibe u(2), d(2), número de iteraciones
ho_lee <- function(u_2, d_2, n, moneda, bern){
  k <- d_2/u_2
  q <- (1-d_2)/(u_2 - d_2)
  r_0 <- -log(forward(0, 0, 1/12, moneda))
  U <- 0
  S <- 0
  # Seguir un camino
  resultados <- data.frame(periodo = 1:(n+1),
                           r_t  = rep(NA_real_, n+1),
                           U_t  = rep(NA_real_, n+1),
                           d = rep(NA_real_, n + 1))
  resultados$r_t[1] <- r_0
  resultados$U_t[1] <- 0
  # Excluimos el caso para d(2)
  S <- S + 1/12 #incrementos mensuales
  if(bern[1] == 1){
    U <- U + 1
  }
  resultados$U_t[1+1] <- U
  d <- d_2
  resultados$d[2] <- d
  r_t <- -log(forward(0, S, S+1/12, moneda)) - log(d) + U*log(k)
  resultados$r_t[1+1] <- r_t
  for(i in 2:n){
    S <- S + 1/12 #incrementos mensuales
    if(bern[i] == 1){
      U <- U + 1
    }
    resultados$U_t[i+1] <- U
    d <- k^(i)/((1-q)*k^(i) + q) #d(t + 1)
    resultados$d[i+1] <- d
    r_t <- -log(forward(0, S, S+1/12, moneda)) - log(d) + U*log(k)
    resultados$r_t[i+1] <- r_t
  }
  return(resultados)
}
```

## Precios
```{r}
precios_arriba <- function(u_2, d_2, n, moneda){
  k <- d_2/u_2
  q <- (1-d_2)/(u_2 - d_2)
  resultados <- data.frame(periodo = 0:n,
                           precio  = rep(NA_real_, n+1),
                           U_t  = 0:n)
  # Se construye la rama de arriba
  u <- function(t){
    u <- 1/((1-q)*k^(t) + q)
  }
  # Separamos las primeras tres
  resultados$precio[1] <- forward(0, 0, n/12, moneda) #Precio actual
  resultados$precio[2] <- u(n)*forward(0, 1/12, n/12, moneda) #Primer salto
  resultados$precio[3] <- u(n - 1)*resultados$precio[2]/(u_2*forward(0, 1/12, 2/12, moneda)) #Segundo salto
  i <- 3
  while(n - i > 1){
    t <- i/12
    resultados$precio[i + 1] <- u(n - i + 1)*resultados$precio[i]/(u(i)*forward(0, t - 1/12, t, moneda))
    i <- i + 1
  }
  resultados$precio[n] <- u_2*resultados$precio[n - 1]/(u(n - 1)*forward(0, n - 2/12, n - 1/12, moneda))
  resultados$precio[n + 1] <- 1
  return(resultados)
}
```

```{r}
otros_precios <- function(u_2, d_2, n, moneda, omegas){
  sup <- precios_arriba(u_2, d_2, n, moneda)
  k <- d_2/u_2
  q <- (1-d_2)/(u_2 - d_2)
  resultados <- data.frame(periodo = 1:(n+1),
                           precio  = sup$precio, #primero y último son iguales
                           U_t  = rep(NA_real_, n+1))
  resultados$U_t[1] <- 0
  for(i in 2:n){
    resultados$U_t[i] <- resultados$U_t[i - 1] + omegas[i - 1]
    resultados$precio[i] <- k^((i - 1 - resultados$U_t[i])*(n - i + 1))*sup$precio[i] #Multiplicar factor la cantidad de veces necesaria
  }
  return(resultados)
}
```


## Árbol Conjunto
```{r}
conjunto <- function(u_2, d_2, n, moneda, omegas, uS, dS, accion){
  res <- ho_lee(u_2, d_2, n, moneda, omegas)
  r <- res$r_t
  k <- d_2/u_2
  q <- (1-d_2)/(u_2 - d_2)
  resultados <- data.frame(periodo = 1:(n+1),
                           S  = rep(NA_real_, n+1),
                           proba_S = rep(NA_real_, n+1),
                           r_t = r,
                           U_t  = res$U_t)
  resultados$S[1] <- accion
  # Separamos la primera iteración
  q_i <- ifelse(exp(r[1]) > uS, (exp(log(uS)) - dS)/(uS - dS), ifelse(exp(r[1]) < dS, (exp(log(dS)) - dS)/(uS - dS), (exp(r[1]) - dS)/(uS - dS)))
  bern <- rbinom(n = 1, size = 1, prob = q_i)
  if(bern == 1){
    resultados$S[2] <- resultados$S[1]*uS
    resultados$proba_S[2] <- q_i
  }
  else{
    resultados$S[2] <- resultados$S[1]*dS
    resultados$proba_S[2] <- 1 - q_i
  }
  for (i in 2:n){
    q_i <- ifelse(exp(r[i]) > uS, (exp(log(uS)) - dS)/(uS - dS), ifelse(exp(r[i]) < dS, (exp(log(dS)) - dS)/(uS - dS), (exp(r[i]) - dS)/(uS - dS)))
    bern <- rbinom(n = 1, size = 1, prob = q_i)
    if(bern == 1){
      resultados$S[i + 1] <- resultados$S[i]*uS
      if(omegas[i - 1] == 1){
        resultados$proba_S[i + 1] <- resultados$proba_S[i]*q_i*q
      }
      else{
        resultados$proba_S[i + 1] <- resultados$proba_S[i]*q_i*(1 - q)
      }
      
    }
    else{
      resultados$S[i + 1] <- resultados$S[i]*dS
      if(omegas[i - 1] == 1){
        resultados$proba_S[i + 1] <- resultados$proba_S[i]*(1 - q_i)*q
      }
      else{
        resultados$proba_S[i + 1] <- resultados$proba_S[i]*(1 - q_i)*(1 - q)
      }
      
    }
  }
  return(resultados)
}
```

## Opción call europea
```{r}
# contrato: precio del contrato, pagado en el momento 0
# K: precio de ejercicio del contrato
# nsim: número de simulaciones
# omega_sim: lista de omegas
# cant: cantidad de acciones
europea <- function(u_2, d_2, n, moneda, omega_sim, uS, dS, accion, contrato, K, nsim, cant){
  sim_S <- data.frame(aporte = rep(NA_real_, nsim))

  for(i in 1:nsim){
    omegas <- omega_sim[[i]]
    camino <- conjunto(u_2, d_2, n, moneda, omegas, uS, dS, accion)
    sim_S$aporte[i] <- cant*(max(camino$S[n + 1] - K, 0)-exp(sum(camino$r_t))*contrato)
  }
  return(sim_S)
}
```


# Valor del portafolio
## Estimación de parámetros
### Colones
Como $u(2) = 1.0001$ y $d(2) = 0.9999$, sabemos que $q = \frac{(1-d(T))}{u(T)-d(T)}$ para cualquier T, entonces $q = 0.5$
```{r}
q <- (1-0.9999)/(1.0001 - 0.9999)
q
```
Para estimar los parámetros de subida  bajada de las acciones, utilizamos el Modelo Cox-Ross-Rubinstein. Por tanto, usamos la fórmula $\sigma^2*\Delta t = \ln(\frac{S_{t + \Delta t}}{S_t})$; en este caso, tanto las acciones como las tasas avanzan mensualmente, por tanto, $\Delta t = 1$
```{r}
S_nac <- readxl::read_excel("../data/precio_acciones_nacionales.xlsx")
```

```{r, warning=FALSE}
library(dplyr)

S_nac <- S_nac %>% arrange(Fecha)

S_nac <- S_nac %>%
  mutate(ret_log = log(Precio / lag(Precio)))

sigma1 <- var(S_nac$ret_log, na.rm = TRUE)
sigma <- sqrt(sigma1)

sigma
```
Ahora, utilizando $u = \frac 1d = e^{\sigma \Delta t}$, obtenemos $u = 1.003618$ y $d = 0.9963954$
```{r}
uS <- exp(sigma)
uS
dS <- exp(-sigma)
dS
```


### Dolares
Como $u(2) = 1.0005$ y $d(2) = 0.99995$, sabemos que $q = \frac{(1-d(T))}{u(T)-d(T)}$ para cualquier T, entonces $q = 0.5$
```{r}
q_dol <- (1-0.99995)/(1.0005 - 0.99995)
q_dol
```
Para estimar los parámetros de subida  bajada de las acciones, utilizamos el Modelo Cox-Ross-Rubinstein. Por tanto, usamos la fórmula $\sigma^2*\Delta t = \ln(\frac{S_{t + \Delta t}}{S_t})$; en este caso, tanto las acciones como las tasas avanzan mensualmente, por tanto, $\Delta t = 1$
```{r}
S_int <- readxl::read_excel("../data/precio_acciones_internacionales.xlsx")
```

```{r, warning=FALSE}
library(dplyr)

S_int <- S_int %>% arrange(Date)

S_int <- S_int %>%
  mutate(ret_log = log(`Close Price` / lag(`Close Price`)))

sigma1 <- var(S_int$ret_log, na.rm = TRUE)
sigma <- sqrt(sigma1)

sigma
```
Ahora, utilizando $u = \frac 1d = e^{\sigma \Delta t}$, obtenemos $u = 1.043396$ y $d = 0.9584087$
```{r}
uS_dol <- exp(sigma)
uS_dol
dS_dol <- exp(-sigma)
dS_dol
```


## Valor esperado teórico en t = 0

```{r}
P_col <- function(T) {
  as.numeric(forward(0, 0, T, "colones"))
}

P_usd <- function(T) {
  as.numeric(forward(0, 0, T, "dolares"))
}
```

### Bonos Cero Cupón

```{r}
# Colones: 5 bonos de ¢10.000.000 a 3, 7, 12, 15 y 20 años
V0_ceros_col <- sum(
  10000000 * P_col(c(3, 7, 12, 15, 20))
)
V0_ceros_col

# Dólares: 3 bonos de $15.000 a 5, 10 y 16 años
V0_ceros_usd <- sum(
  15000 * P_usd(c(5, 10, 16))
)
V0_ceros_usd
```

### Bonos cuponados

```{r}
bono_cuponado <- function(tasa, freq, T, nominal, moneda){
  if(moneda == 'colones') f = P_col
  else f = P_usd
  dt       <- 1 / freq
  tiempos  <- seq(dt, T, by = dt)
  cupon    <- nominal * tasa #asumimos tasa por periodo
  sum(cupon * sapply(tiempos, f)) + nominal * f(T)
}
```

```{r}
# Colones: Mensual de 3% a 10 años, trimestral de 5% a 15 años y anual de 8% a 17 años. 20 millones
mensual <- bono_cuponado(0.03, 12, 10, 20000000, 'colones')
mensual
trimestral <- bono_cuponado(0.05, 4, 15, 20000000, 'colones')
trimestral
anual <- bono_cuponado(0.08, 1, 17, 20000000, 'colones')
anual
total_cuponados_col <- mensual + trimestral + anual

# Dólares: Bianual de 7% a 15 años, 20 mil
bianual <- bono_cuponado(0.07, 0.5, 15, 20000, 'dolares')
bianual
```

### Acciones

```{r}
V0_acc_col <- 500 * 24000    # 500 acciones a ¢24.000
V0_acc_usd <- 300 * 120      # 300 acciones a $120
```

### Valor total por moneda

```{r}
V0_colones <- V0_ceros_col + total_cuponados_col + V0_acc_col
V0_colones
V0_usd <- V0_ceros_usd + bianual + V0_acc_usd
V0_usd
```

### Valor total en colones
Para cambiar en el tiempo 0, usamos el tipo de cambio de la fecha de corte: 31 de octubre del 2025.
Este valor se obtuvo de [tipo de cambio promedio MONEX](https://www.bancopopular.fi.cr/wp-content/uploads/2025/11/Boletin-Local-31-10-2025.pdf?utm_source=chatgpt.com)
Como la empresa es costarricense, usaremos el tipo de cambio de venta, pues esta deberá vender los dólares para obtener colones.
```{r}
E0 <- 504.44 

V0_total_colones <- V0_colones + V0_usd * E0
V0_total_colones
```

```{r}
data.frame(
  Concepto = c("Valor en colones", "Valor en dólares", "Total en colones"),
  Valor    = c(V0_colones, V0_usd, V0_total_colones)
)
```


## Valor esperado basado en simulaciones

```{r}
n_sims  <- 10000
n_pasos <- 15 * 12
S0_ne  <- 24000
u2_col <- 1.0001
d2_col <- 0.9999
q_col  <- (1 - d2_col) / (u2_col - d2_col)
S0_dol <- 120
u2_dol <- 1.00005
d2_dol <- 0.99995
q_dol <- (1 - d2_dol) / (u2_dol - d2_dol)
omega_sim_col <- replicate(
  n_sims,
  rbinom(n = n_pasos, size = 1, prob = q_col),
  simplify = FALSE
)
omega_sim_dol <- replicate(
  n_sims,
  rbinom(n = n_pasos, size = 1, prob = q_dol),
  simplify = FALSE
)
horizontes <- c(2.5, 10, 15)
pasos_h    <- as.integer(horizontes * 12)
```

### Acciones
```{r}
V_port_path <- matrix(NA_real_, nrow = n_sims, ncol = length(horizontes))
colnames(V_port_path) <- paste0("t_", horizontes)

n_h <- length(horizontes)

V_acc_col_path <- matrix(NA_real_, nrow = n_sims, ncol = n_h)
V_acc_dol_path <- matrix(NA_real_, nrow = n_sims, ncol = n_h)

for (i in seq_len(n_sims)) {
  # acciones nacionales
  camino_col_i <- conjunto(
    u_2    = u2_col,
    d_2    = d2_col,
    n      = n_pasos,           
    moneda = "colones",
    omegas = omega_sim_col[[i]],
    uS     = uS,
    dS     = dS,
    accion = S0_ne              
  )
  
  # acciones internacionales
  camino_dol_i <- conjunto(
    u_2    = u2_dol,
    d_2    = d2_dol,
    n      = n_pasos,           
    moneda = "dolares",
    omegas = omega_sim_dol[[i]],
    uS     = uS_dol,
    dS     = dS_dol,
    accion = S0_dol              
  )
  
  for (j in seq_along(horizontes)) {
    idx_t <- pasos_h[j] + 1L     
    S_col_t <- camino_col_i$S[idx_t]
    S_dol_t <- camino_dol_i$S[idx_t]
    V_acc_col_path[i, j] <- 500 * S_col_t
    V_acc_dol_path[i, j] <- 300 * S_dol_t 
  }
}


resumen_acciones <- data.frame(
  horizonte = horizontes,
  mean_col  = colMeans(V_acc_col_path),
  sd_col    = apply(V_acc_col_path,  2, sd),
  mean_dol  = colMeans(V_acc_dol_path),
  sd_dol    = apply(V_acc_dol_path,  2, sd)
)
resumen_acciones
write.csv(
  resumen_acciones,
  file = "../res/resumen_acciones.csv",
  row.names = FALSE
)
```

### Bonos cero cupón
### Colones
```{r}
T_bcc_col   <- c(3, 7, 12, 15, 20)
n_bonos_col   <- length(T_bcc_col)
n_h           <- length(horizontes)
nominal <- 10000000
precio_bcc_col <- lapply(seq_len(n_bonos_col), function(k) {
  matrix(NA_real_, nrow = n_sims, ncol = n_h)
})
names(precio_bcc_col) <- paste0("T_", T_bcc_col)

for (i in seq_len(n_sims)) {
  
  tasas_colones <- ho_lee(u2_col, d2_col, n_pasos, "colones", omega_sim_col[[i]])
  
  for (k in seq_along(T_bcc_col)) {
    T_bono  <- T_bcc_col[k]
    n_bono  <- as.integer(T_bono * 12)         
    idx_venc <- n_bono + 1L                    
    
    col_bono <- otros_precios(
      u_2    = u2_col,
      d_2    = d2_col,
      n      = n_bono,
      moneda = "colones",
      omegas = omega_sim_col[[i]]
    )
    
    for (j in seq_along(horizontes)) {
      idx <- pasos_h[j] + 1L   
      
      if (idx <= idx_venc) { # Si no ha vencido
        precio_bcc_col[[k]][i, j] <- nominal*col_bono$precio[idx]
      } else { # Si ya venció
        precio_bcc_col[[k]][i, j] <- nominal*exp(sum(tasas_colones$r_t[(idx_venc + 1L):idx]))
      }
    }
  }
}
resumen_bcc_col <- bind_rows(lapply(seq_along(T_bcc_col), function(k) {
  data.frame(
    T_bono    = T_bcc_col[k],
    horizonte = horizontes,
    mean_val  = colMeans(precio_bcc_col[[k]]),
    sd_val    = apply(precio_bcc_col[[k]], 2, sd)
  )
}))
resumen_bcc_col

total_bcc_col_path <- Reduce("+", precio_bcc_col) #suma de precios por horizonte, por simulación

resumen_bcc_col_total <- data.frame(
  horizonte = horizontes,
  mean_val  = colMeans(total_bcc_col_path),
  sd_val    = apply(total_bcc_col_path, 2, sd)
)

resumen_bcc_col_total
write.csv(
  resumen_bcc_col_total,
  file = "../res/resumen_bcc_col_total.csv",
  row.names = FALSE
)
```

### Dólares
```{r}
T_bcc_dol   <- c(5, 10, 16)
n_bonos_dol   <- length(T_bcc_dol)
n_h           <- length(horizontes)
nominal <- 15000
precio_bcc_dol <- lapply(seq_len(n_bonos_dol), function(k) {
  matrix(NA_real_, nrow = n_sims, ncol = n_h)
})
names(precio_bcc_dol) <- paste0("T_", T_bcc_dol)

for (i in seq_len(n_sims)) {
  
  tasas_dolones <- ho_lee(u2_dol, d2_dol, n_pasos, "dolares", omega_sim_dol[[i]])
  
  for (k in seq_along(T_bcc_dol)) {
    T_bono  <- T_bcc_dol[k]
    n_bono  <- as.integer(T_bono * 12)         
    idx_venc <- n_bono + 1L                    
    
    dol_bono <- otros_precios(
      u_2    = u2_dol,
      d_2    = d2_dol,
      n      = n_bono,
      moneda = "dolares",
      omegas = omega_sim_dol[[i]]
    )
    
    for (j in seq_along(horizontes)) {
      idx <- pasos_h[j] + 1L   
      
      if (idx <= idx_venc) { # Si no ha vencido
        precio_bcc_dol[[k]][i, j] <- nominal*dol_bono$precio[idx]
      } else { # Si ya venció
        precio_bcc_dol[[k]][i, j] <- nominal*exp(sum(tasas_dolones$r_t[(idx_venc + 1L):idx]))
      }
    }
  }
}
resumen_bcc_dol <- bind_rows(lapply(seq_along(T_bcc_dol), function(k) {
  data.frame(
    T_bono    = T_bcc_dol[k],
    horizonte = horizontes,
    mean_val  = colMeans(precio_bcc_dol[[k]]),
    sd_val    = apply(precio_bcc_dol[[k]], 2, sd)
  )
}))
resumen_bcc_dol

total_bcc_dol_path <- Reduce("+", precio_bcc_dol) #suma de precios por horizonte, por simulación

resumen_bcc_dol_total <- data.frame(
  horizonte = horizontes,
  mean_val  = colMeans(total_bcc_dol_path),
  sd_val    = apply(total_bcc_dol_path, 2, sd)
)

resumen_bcc_dol_total
write.csv(
  resumen_bcc_dol_total,
  file = "../res/resumen_bcc_dol_total.csv",
  row.names = FALSE
)
```

### Bonos cuponados

```{r}
simular_bonos_cuponados <- function(T_bonos, tasa, freq, nominal,
                                       u2, d2, n_pasos,
                                       omega_sim, horizontes,
                                       moneda) {
  n_sims  <- length(omega_sim)
  n_bonos <- length(T_bonos)
  n_h     <- length(horizontes)
  
  precio_bc <- lapply(seq_len(n_bonos), function(k) {
    matrix(NA_real_, nrow = n_sims, ncol = n_h)
  })
  names(precio_bc) <- paste0("T_", T_bonos)
  
  for (i in seq_len(n_sims)) {
    
    tasas_i <- ho_lee(u2, d2, n_pasos, moneda, omega_sim[[i]])
    r_path  <- tasas_i$r_t
    
    for (k in seq_along(T_bonos)){
      T_k    <- T_bonos[k]   
      tasa_k <- tasa[k]       
      freq_k <- freq[k]       
      
      dt_pay  <- 1 / freq_k
      tiempos <- seq(dt_pay, T_k, by = dt_pay)      
      cupon   <- nominal * tasa_k                  
      
      CF_amounts <- rep(cupon, length(tiempos))
      CF_amounts[length(CF_amounts)] <- cupon + nominal #último pago
      
      CF_months <- as.integer(round(tiempos * 12))
      CF_idx    <- CF_months + 1L 
      
      for (j in seq_along(horizontes)) {
        S    <- horizontes[j]
        Sm   <- as.integer(round(S * 12))
        idxS <- Sm + 1L
        
        valor_S <- 0
        
        for (l in seq_along(tiempos)) {
          t_cf    <- tiempos[l]
          idx_cf  <- CF_idx[l]
          C_fl    <- CF_amounts[l]
          
          if (t_cf >= S){ #No han pagado cupón
            n_cf <- CF_months[l]
            precios_cf <- otros_precios(
              u_2    = u2,
              d_2    = d2,
              n      = n_cf,
              moneda = moneda,
              omegas = omega_sim[[i]]
            )
            
            P_S_cf <- precios_cf$precio[idxS]
            
            valor_S <- valor_S + C_fl * P_S_cf
            
          } else{ #Ya pagado
            valor_S <- valor_S + C_fl * exp(sum(r_path[(idx_cf + 1L):idxS]))
          }
        }
        precio_bc[[k]][i, j] <- valor_S
      }
    }
  }
  
  # Suma de todos los cuponados por simulación y horizonte
  total_bc_path <- Reduce("+", precio_bc)
  
  resumen_bc_total <- data.frame(
    horizonte = horizontes,
    mean_val  = colMeans(total_bc_path),
    sd_val    = apply(total_bc_path, 2, sd)
  )
  
  list(
    precio_por_bono = precio_bc,      
    total_path      = total_bc_path,  
    resumen_total   = resumen_bc_total
  )
}
```

```{r}
# Colones: mensual 3% a 10 años, trimestral 5% a 15 años, anual 8% a 17 años, todos de 20 millones
T_bc_col    <- c(10, 15, 17)
tasa_bc_col <- c(0.03, 0.05, 0.08)
freq_bc_col <- c(12, 4, 1)
nominal_bc  <- 20000000

res_bc_col <- simular_bonos_cuponados(
  T_bonos    = T_bc_col,
  tasa       = tasa_bc_col,
  freq       = freq_bc_col,
  nominal    = nominal_bc,
  u2         = u2_col,
  d2         = d2_col,
  n_pasos    = n_pasos,         
  omega_sim  = omega_sim_col,   
  horizontes = horizontes,      
  moneda     = "colones"
)

res_bc_col$resumen_total
write.csv(
  res_bc_col$resumen_total,
  file = "../res/cuponados_colones.csv",
  row.names = FALSE
)
```


```{r}
# Dólares: Bianual de 7% a 15 años, 20 mil
T_bc_dol    <- c(15)
tasa_bc_dol <- c(0.07)
freq_bc_dol <- c(0.5)
nominal_bc  <- 20000

res_bc_dol <- simular_bonos_cuponados(
  T_bonos    = T_bc_dol,
  tasa       = tasa_bc_dol,
  freq       = freq_bc_dol,
  nominal    = nominal_bc,
  u2         = u2_dol,
  d2         = d2_dol,
  n_pasos    = n_pasos,         
  omega_sim  = omega_sim_dol,   
  horizontes = horizontes,      
  moneda     = "dolares"
)

res_bc_dol$resumen_total
write.csv(
  res_bc_dol$resumen_total,
  file = "../res/cuponados_dolares.csv",
  row.names = FALSE
)
```

### Valor total
```{r}
## Tipo de cambio por horizonte
tc_h <- E0 * sapply(horizontes, function(T) P_usd(T) / P_col(T))

## Componentes en colones
val_acc_col   <- resumen_acciones$mean_col
val_bcc_col   <- resumen_bcc_col_total$mean_val
val_cup_col   <- res_bc_col$resumen_total$mean_val

## Componentes en dólares
val_acc_dol   <- resumen_acciones$mean_dol
val_bcc_dol   <- resumen_bcc_dol_total$mean_val
val_cup_dol   <- res_bc_dol$resumen_total$mean_val

## Totales por horizonte
total_colones <- val_acc_col + val_bcc_col + val_cup_col
total_dolares <- val_acc_dol + val_bcc_dol + val_cup_dol

## Data frame final
resumen_portafolio <- data.frame(
  horizonte     = horizontes,
  colones       = total_colones,
  dolares       = total_dolares,
  tipo_cambio   = tc_h,
  total_colones = total_colones + total_dolares * tc_h
)

fila_0 <- data.frame(
  horizonte     = 0,
  colones       = V0_colones,
  dolares       = V0_usd,
  tipo_cambio   = E0,
  total_colones = V0_total_colones
)

resumen_portafolio <- rbind(fila_0, resumen_portafolio)

resumen_portafolio


write.csv(
  resumen_portafolio,
  file = "../res/resumen_portafolio.csv",
  row.names = FALSE
)
```

## Análisis de Riesgo
### Pérdida
```{r}
tc_mat <- matrix(tc_h, nrow = n_sims, ncol = length(horizontes), byrow = TRUE)

valor_col_path <- V_acc_col_path +
                  total_bcc_col_path +
                  res_bc_col$total_path

valor_dol_path <- V_acc_dol_path +
                  total_bcc_dol_path +
                  res_bc_dol$total_path

V_port_total_col_path <- valor_col_path + valor_dol_path * tc_mat

precio_teorico <- colMeans(V_port_total_col_path)

benchmark_mat <- matrix(
  precio_teorico,
  nrow = n_sims,
  ncol = length(horizontes),
  byrow = TRUE
)


loss_path <- benchmark_mat - V_port_total_col_path # teórico - escenario
```

```{r}
p_pes  <- 0.95
n_h    <- ncol(loss_path)

VaR_95  <- numeric(n_h)
CVaR_95 <- numeric(n_h)

for (j in seq_len(n_h)) {
  L <- loss_path[, j]
  VaR_95[j]  <- as.numeric(quantile(L, probs = p_pes, names = FALSE))
  CVaR_95[j] <- mean(L[L >= VaR_95[j]]) 
}

# Escenario optimista: VaR y "CVaR" al 30% usando la cola inferior
p_opt <- 0.30

VaR_30  <- numeric(n_h)
CVaR_30 <- numeric(n_h)

for (j in seq_len(n_h)) {
  L <- loss_path[, j]
  VaR_30[j]  <- as.numeric(quantile(L, probs = p_opt, names = FALSE))
  CVaR_30[j] <- mean(L[L <= VaR_30[j]])  # cola inferior (escenario optimista)
}

resumen_riesgo <- data.frame(
  horizonte = horizontes,
  VaR_95    = VaR_95,
  CVaR_95   = CVaR_95,
  VaR_30    = VaR_30,
  CVaR_30   = CVaR_30
)

resumen_riesgo
```

```{r}
resumen_riesgo <- resumen_portafolio %>%
  left_join(resumen_riesgo, by = "horizonte")

resumen_riesgo
write.csv(
  resumen_riesgo,
  file = "../res/resumen_pérdidas.csv",
  row.names = FALSE
)
```

### Valor del portafolio
```{r}
V_mat <- V_port_total_col_path
n_h   <- ncol(V_mat)


# Escenario pesimista

p_pes    <- 0.95
alpha_pes <- 1 - p_pes  

VaR_95_val  <- numeric(n_h)
CVaR_95_val <- numeric(n_h)

for (j in seq_len(n_h)) {
  Vj <- V_mat[, j]  
  VaR_95_val[j]  <- as.numeric(quantile(Vj, probs = alpha_pes, names = FALSE))
  CVaR_95_val[j] <- mean(Vj[Vj <= VaR_95_val[j]])
}

# Escenario optimista

p_opt     <- 0.30
alpha_opt <- 1 - p_opt  

VaR_30_val  <- numeric(n_h)
CVaR_30_val <- numeric(n_h)

for (j in seq_len(n_h)) {
  Vj <- V_mat[, j]
  VaR_30_val[j]  <- as.numeric(quantile(Vj, probs = alpha_opt, names = FALSE))
  CVaR_30_val[j] <- mean(Vj[Vj >= VaR_30_val[j]])
}

```

```{r}
riesgo_port_df <- data.frame(
  horizonte   = horizontes,
  VaR_95_val  = VaR_95_val,
  CVaR_95_val = CVaR_95_val,
  VaR_30_val  = VaR_30_val,
  CVaR_30_val = CVaR_30_val
)

riesgo_port_df

```

```{r}
riesgo_port_df <- resumen_portafolio %>%
  left_join(riesgo_port_df, by = "horizonte")

riesgo_port_df
write.csv(
  riesgo_port_df,
  file = "../res/resumen_riesgo.csv",
  row.names = FALSE
)
```



# Sección II: Análisis de expansión accionaria

## Compra directa
```{r}
S15_ne <- numeric(n_sims)

for (i in 1:n_sims) {
  camino_i <- conjunto(
    u_2    = u2_col,
    d_2    = d2_col,
    n      = n_pasos,
    moneda = "colones",
    omegas = omega_sim_col[[i]],  
    uS     = uS,                  
    dS     = dS,
    accion = S0_ne
  )
  
  S15_ne[i] <- camino_i$S[nrow(camino_i)]
}

aporte_directa <- 200 * S15_ne

resumen_2a <- c(
  media = mean(aporte_directa),
  sd    = sd(aporte_directa)
)

resumen_2a
```

## Compra de una opción call europea 

```{r}
Kf_ne      <- S0_ne * exp(15 * 12 * 0.0035)
K_call_eur <- Kf_ne - 5000

n_calls         <- 200
precio_0_call   <- 0 #no tomamos en cuenta el precio del contrato 
res_eur_2d <- europea(
  u_2       = u2_col,
  d_2       = d2_col,
  n         = n_pasos,
  moneda    = "colones",
  omega_sim = omega_sim_col,  
  uS        = uS,             
  dS        = dS,
  accion    = S0_ne,
  contrato  = precio_0_call,  
  K         = K_call_eur,
  nsim      = n_sims,
  cant      = n_calls
)

resumen_2d <- c(
  media = mean(res_eur_2d$aporte),
  sd    = sd(res_eur_2d$aporte)
)

resumen_2d
```


```{r}
resumen_comparacion <- data.frame(
  Estrategia = c("Compra directa", "Opción Call Europea"),
  media      = c(resumen_2a["media"], resumen_2d["media"]),
  sd         = c(resumen_2a["sd"],    resumen_2d["sd"])
)

resumen_comparacion

write.csv(
  resumen_comparacion,
  file = "../res/expansion_accionaria.csv",
  row.names = FALSE
)
```

